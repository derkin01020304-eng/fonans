# Результаты проверки

Обновлено: 8 сентября 2026. Версия: 0.1.1+2. Среды: Linux x64 и эмулятор Android API 35, Flutter 3.35.7, Dart 3.9.2.

## Подтверждённая сборка Android

[GitHub Actions 34163728654](https://github.com/derkin01020304-eng/fonans/actions/runs/34163728654) завершился успешно 7 сентября 2026 в 21:48 UTC. Исходный commit: `2b16ab94355f53aba06abdcac3feb4ae59415dd8`.

- `flutter analyze --no-fatal-infos`: успешный выход, 102 информационных lint-замечания, без ошибок и предупреждений.
- `flutter test`: 44 теста пройдены.
- `integration_test/android_storage_test.dart`: один нативный тест SQLCipher/Keystore пройден на эмуляторе Android API 35.
- `flutter build apk --release`: успешно, APK размером 74 041 564 байта, подписан отладочным ключом runner согласно настройкам проекта.
- Артефакт `derk-finance-apk`, ID `10033676498`; хранится до 21 сентября 2026.
- ZIP скачан, его SHA-256 совпал с GitHub; структура извлечённого APK проверена без ошибок.

SHA-256 APK (`app-release.apk`, передан пользователю как `derk_finance_0.1.1.apk`):

    d140677ff4c24d1405b3322e28aec96c62f00dc4d363c68b5cf79fa5ae2d24b8

SHA-256 ZIP-артефакта:

    7e69b0000db4cc4a1c05c62ed46327366615210ca35f4f6db9847095e5c8e018

Установка именно release-APK на физический телефон пока не подтверждена. Нативный интеграционный тест выполнялся в debug-сборке с производственными настройками базы. Ключ подписи предыдущего пользовательского APK неизвестен, поэтому совместимость установки поверх него не подтверждена.

## Выполнено

| Проверка | Результат |
|---|---|
| Получение зависимостей | Успешно; версии закреплены pubspec.lock |
| json_serializable / build_runner | Успешно; models.g.dart с paymentDay включён |
| Flutter unit + widget tests | **44 теста пройдены, 0 ошибок** |
| Проверка типов и диагностик через analyzer 7.7.1 | **0 errors, 0 warnings** |
| Dart formatter | Исходники отформатированы |
| Python stdio-мост MCP | py_compile успешно |
| Интерфейс | Реальные виджеты Flutter отрисованы; проверены пять вкладок, дополнительные страницы, форма расхода, светлая и тёмная тема |

Полный прогон:

    flutter test --no-pub --reporter expanded

Итог:

    00:13 +44: All tests passed!

Для Linux использовалась системная SQLite через FFI. Нативные вызовы Android в widget-тестах подменены; финансовые операции тестов выполняются на настоящей временной SQLite, с рабочими миграциями и внешними ключами. Для снимков использованы шрифты из Flutter SDK. Изображения в docs/previews — рендер Flutter-тестов, не скриншоты физического телефона.

## Что покрывают тесты

- Точное преобразование денег и границы дат.
- Сохранение общего баланса при переводах.
- Пополнение/снятие накоплений и остаток цели.
- Сохранение банковского externalKey при переводе в накопления.
- Запрет переводов между разными валютами и одинаковыми счетами.
- Изменение/удаление обычной операции без накопления ошибок баланса.
- Частичное погашение долга и запрет переплаты.
- Возврат долга без ложного дохода.
- Раздельный учёт тела кредита и процентов.
- Атомарный rollback при ошибочном счёте или восстановлении.
- Сохранение 31-го дня платежа после февраля.
- Все еженедельные платежи в горизонте 30 дней.
- Однократное вычитание зарезервированной суммы.
- Почасовой доход только по операциям с указанным временем.
- Приоритет подтверждённой категории.
- Вход и выход из демо с сохранением первоначального состояния.
- CSV, XLSX, OFX; десятичная запятая, часовые пояса, ошибочные строки.
- Повтор выписки и повтор одинаковых строк без банковского ID.
- Идемпотентность mock-банка и согласование начального остатка.
- AES-GCM: расшифровка, неверный пароль, изменение ciphertext.
- Миграция базы v1 → v2.
- JSON Schema: целые суммы, диапазоны, неверные даты и лишние поля.
- API: запрет доступа без разрешения, запрет повышения scope, отзыв токена.
- Повторный параллельный requestId создаёт только одну операцию.
- MCP: initialize, scoped tools/list, structuredContent, отказ чужому Origin, неверному токену и GET.
- PIN: соль/верификатор, смена PIN, блокировка после пяти неудачных попыток.
- Уход в фон во время проверки PIN не разблокирует приложение.
- Блокировка через две минуты отзывает разрешение AI.
- Навигация на экране шириной 320 px и реальное сохранение расхода через UI на 390 px.
- Настройка базы через общий с приложением код; Android-контракт запрещает выполнять возвращающий строки PRAGMA через execute. FFI-адаптер воспроизводит именно это ограничение драйвера.
- Коды ошибок запуска не раскрывают SQL и ключи из нативного исключения.
- Экран ошибки отображает копируемый код и позволяет повторить запуск.

## Ограничения проверки

В первоначальной локальной среде стандартный `flutter analyze` не завершился из-за SIGBUS в snapshot analysis server SDK. Тогда выполнен `dart run scripts/analyze_local.dart`, проверяющий типы, ошибки и предупреждения без Flutter lint rules. Затем стандартная проверка успешно выполнена в GitHub Actions:

    flutter analyze --no-fatal-infos

Ошибки и предупреждения остаются блокирующими; информационные stylistic lint-сообщения не блокируют предоставленные сценарии сборки.

**APK 0.1.1+2 собран; AAB не собирался.** Первоначальная блокировка доступа к репозиторию устранена, проект загружен в `derkin01020304-eng/fonans`, Gradle/JNI-компиляция подтверждена успешным workflow. Пользователь сообщил об установке предыдущего APK; его исходный commit и журнал запуска пока недоступны.

Не проверялись на физическом устройстве: биометрия, OAuth реального банка, системные файловые диалоги, listener банковских уведомлений и перезагрузка с восстановлением alarms. Google Play-публикация не выполнялась.

## Проверка Android при сборке

    flutter doctor -v
    flutter pub get
    flutter analyze --no-fatal-infos
    flutter test
    flutter test integration_test/android_storage_test.dart -d DEVICE_ID
    flutter build apk --release

Нативный интеграционный тест использует собственный временный SQLCipher-файл и отдельный тестовый ключ защищённого хранилища. В 0.1.1 он открывает базу через настоящий FinanceDatabase.openEncrypted, применяет общие настройки и миграции, создаёт начальные счета и записывает операцию. Проверяет PRAGMA cipher_version, foreign_keys и secure_delete, отсутствие обычного SQLite-заголовка, сохранность операции после повторного открытия и очистку только тестовых данных. Этот тест прошёл в GitHub Actions на эмуляторе Android API 35.

Workflow `.github/workflows/android.yml` включает эмулятор Android API 35 и требует успешного нативного теста перед сборкой APK. Все его этапы успешно выполнены в указанном выше запуске.

Инструкции установки SDK и подписи APK находятся в README.md. Внешние банковские/AI credentials в проекте отсутствуют. Реальный банк, PDF/OCR и облачная синхронизация не заявлены как выполненные интеграции.
